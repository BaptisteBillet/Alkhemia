using UnityEngine;
using System.Collections;

public class MouseScriptApparence : MonoBehaviour {

	//public Sprite sprite;
	
	// Use this for initialization
	void Start () {
        //Cursor.visible = false;
	
	}
    
    void OnGUI ()
    {
        //GUI.Label(Rect(Input.mousePosition.x, Input.mousePosition.y, 16, 16), sprite );
    } 
}
